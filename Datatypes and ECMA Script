"use strict"// treat all JS code as newer version 
alert() // give error on running it on Node but it run on Browser or in HTML
ECMA SCRIPT : it is a standard to write the JavaScript code 
DataTypes::
           1.number : 2 to power 53 range
           2.bigint
           3.string
           4.boolean
           5.null == standalone value
           6.undefined == khali hai

typeOf(VariableName) // define the type of variable
typeOf(Undefined) // undefined
typeOf(null) // Object

let a = "30";
typeOf(a) // string;
Number(a);
typeOf(a) // number;

let a = "30anc"
Number(a);
typeOf(a) //number;
console.log(a) // NAN not a number
if a = null then 0
if a = undefined then NAN
if true then 1 and false then 0
